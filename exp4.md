# 实验4
复现 Table 1 的核心在于验证“注意力池（Attention Sink）”的存在，并证明仅仅保留前几个 token（甚至将它们替换为无语义的换行符）就能防止模型在长文本生成时崩溃 。


### 实验目标

在 PG19 测试集的第一本书（包含 65K tokens）上 ，评估 Llama-2-7B 模型在不同 KV Cache 截断策略下的语言建模困惑度（Perplexity, PPL） 。

---


###  核心实验分组设定

你需要实现并对比以下三种 KV Cache 配置，保证总缓存大小始终固定为 1024 ：

1. **对照组 1：0+1024 (Window Attention)**
* 
**策略:** 纯滑动窗口。仅保留最近的 1024 个 token 的 KV states 。


* 
**预期:** 初始 token 被挤出后，模型崩溃，PPL 将极高（论文结果为 5158.07） 。




2. **实验组 1：4+1020 (StreamingLLM)**
* 
**策略:** 保留最初的 4 个 token 作为 Attention Sink，加上最近的 1020 个 token 的 KV states 。


* 
**预期:** 模型恢复正常能力，PPL 下降至健康水平（论文结果为 5.40） 。




3. **实验组 2：4"\n"+1020 (换行符替换)**
* 
**策略:** 将输入文本的最前 4 个 token 强制替换为换行符（`\n`）的 token ID，保留这 4 个换行符的 KV states，加上最近的 1020 个 token 。


* 
**预期:** 证明 Attention Sink 的作用主要来自于“绝对位置”而非“语义内容” 。PPL 应与实验组 1 相当（论文结果为 5.60） 。



---

###  验证与指标计算

* **困惑度计算:** 对 65K tokens 的序列逐步进行前向传播，收集每个 step 的 `CrossEntropyLoss`（即对真实下一个 token 的负对数似然）。
* 将所有步骤的 Loss 求平均后取指数 ($e^{\text{Average Loss}}$)，即可得到该配置下的序列 PPL。

